import fn from "../src/";
import { expect } from "chai";

describe("leet code ", () => {

  it("case 1", () => {
    expect(fn()).to.equal();
  })

  it("case 2", () => {
    expect(fn()).to.equal();
  })

})